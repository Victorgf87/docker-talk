apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chartito.fullname" . }}-test-connection"
  labels:
    {{- include "chartito.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chartito.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
