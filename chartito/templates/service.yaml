apiVersion: v1
kind: Service
metadata:
  name: {{ include "chartito.fullname" . }}
  labels:
    {{- include "chartito.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chartito.selectorLabels" . | nindent 4 }}
